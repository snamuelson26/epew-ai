"use client";

import { FormEvent, useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";

type Member={id:string;organization_id:string;display_name:string;title:string;role_code:string;permissions:Record<string,boolean>};
type Message={id:string;sender_member_id:string;message_type:string;subject:string|null;body:string;assignment_due_at:string|null;follow_up_at:string|null;attachments:Array<{name:string;path:string}>;created_at:string;sender?:{display_name:string;title:string}};

export default function EmanonCommunicationCenter(){
 const [member,setMember]=useState<Member|null>(null);const [conversationId,setConversationId]=useState("");const [messages,setMessages]=useState<Message[]>([]);const [subject,setSubject]=useState("");const [body,setBody]=useState("");const [type,setType]=useState("message");const [due,setDue]=useState("");const [followUp,setFollowUp]=useState("");const [files,setFiles]=useState<FileList|null>(null);const [notice,setNotice]=useState("");const [busy,setBusy]=useState(false);
 useEffect(()=>{void load();},[]);
 async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){window.location.href="/emanon/login";return;}const {data:m}=await supabase.from("emanon_staff_members").select("id,organization_id,display_name,title,role_code,permissions").eq("user_id",user.id).eq("status","active").single();if(!m){window.location.href="/emanon/login";return;}setMember(m);const {data:links}=await supabase.from("emanon_conversation_members").select("conversation_id").eq("member_id",m.id).limit(1);const cid=links?.[0]?.conversation_id;if(cid){setConversationId(cid);await loadMessages(cid,m.id);}}
 async function loadMessages(cid:string,memberId:string){const {data,error}=await supabase.from("emanon_messages").select("*,sender:emanon_staff_members!sender_member_id(display_name,title)").eq("conversation_id",cid).order("created_at");if(error){setNotice(error.message);return;}setMessages((data??[]) as Message[]);const incoming=(data??[]).filter((item)=>item.sender_member_id!==memberId).map((item)=>({message_id:item.id,member_id:memberId,opened_at:new Date().toISOString()}));if(incoming.length)await supabase.from("emanon_message_receipts").upsert(incoming,{onConflict:"message_id,member_id"});}
 async function submit(event:FormEvent){event.preventDefault();if(!member||!conversationId||!body.trim())return;setBusy(true);setNotice("");const attachments:Array<{name:string;path:string}>=[];for(const file of Array.from(files??[])){const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const path=`${member.organization_id}/${member.id}/${crypto.randomUUID()}-${safe}`;const {error}=await supabase.storage.from("emanon-communications").upload(path,file);if(error){setBusy(false);return setNotice(`Attachment failed: ${error.message}`);}attachments.push({name:file.name,path});}const {error}=await supabase.from("emanon_messages").insert({organization_id:member.organization_id,conversation_id:conversationId,sender_member_id:member.id,message_type:type,subject:subject.trim()||null,body:body.trim(),assignment_due_at:type==="assignment"&&due?new Date(due).toISOString():null,follow_up_at:followUp?new Date(followUp).toISOString():null,attachments});if(error){setBusy(false);return setNotice(error.message);}setSubject("");setBody("");setDue("");setFollowUp("");setFiles(null);setBusy(false);await loadMessages(conversationId,member.id);}
 async function download(path:string){const {data,error}=await supabase.storage.from("emanon-communications").createSignedUrl(path,120);if(error)return setNotice(error.message);window.open(data.signedUrl,"_blank","noopener,noreferrer");}
 if(!member)return <main className="min-h-screen bg-[#f5f7fb] p-8 text-center text-2xl font-bold text-[#06245c]">Loading Emanon Communication Center...</main>;
 const isDirector=member.role_code==="program_director";
 return <main className="min-h-screen bg-[#f5f7fb] p-4 text-[#06245c] md:p-8"><div className="mx-auto max-w-6xl"><header className="mb-6 rounded-3xl bg-white p-6 shadow"><p className="text-sm font-bold uppercase tracking-widest text-green-700">Emanon Institute · Innovative Educational System</p><h1 className="text-4xl font-extrabold">Communication Center</h1><p className="mt-2 text-gray-700">{member.display_name} · {member.title}</p></header>{!conversationId&&<section className="rounded-2xl bg-amber-50 p-6 font-semibold text-amber-900">Your secure account is active. The direct conversation will appear automatically after the other authorized Emanon staff member activates their account.</section>}{conversationId&&<div className="grid gap-6 lg:grid-cols-[1fr_420px]"><section className="rounded-3xl bg-white p-6 shadow"><h2 className="mb-5 text-2xl font-bold">Full Conversation History</h2><div className="max-h-[650px] space-y-4 overflow-y-auto">{messages.length===0&&<p className="text-gray-600">No messages yet.</p>}{messages.map((message)=><article key={message.id} className={`rounded-2xl border p-4 ${message.sender_member_id===member.id?"border-blue-200 bg-blue-50":"border-green-200 bg-green-50"}`}><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><p className="font-bold">{message.sender?.display_name??"Emanon Staff"}</p><span className="rounded-full bg-white px-3 py-1 text-xs font-bold uppercase">{message.message_type.replace("_"," ")}</span></div>{message.subject&&<h3 className="font-bold">{message.subject}</h3>}<p className="whitespace-pre-wrap text-gray-800">{message.body}</p>{message.assignment_due_at&&<p className="mt-2 text-sm font-semibold">Due: {new Date(message.assignment_due_at).toLocaleString()}</p>}{message.follow_up_at&&<p className="mt-1 text-sm font-semibold">Follow-up: {new Date(message.follow_up_at).toLocaleString()}</p>}{message.attachments?.map((file)=><button key={file.path} onClick={()=>void download(file.path)} className="mr-2 mt-3 rounded-lg bg-white px-3 py-2 text-sm font-bold underline">Open {file.name}</button>)}<p className="mt-3 text-xs text-gray-500">{new Date(message.created_at).toLocaleString()}</p></article>)}</div></section><section className="rounded-3xl bg-white p-6 shadow"><h2 className="mb-5 text-2xl font-bold">New Communication</h2>{notice&&<p className="mb-4 rounded-xl bg-amber-50 p-3 font-semibold text-amber-900">{notice}</p>}<form onSubmit={submit} className="space-y-4"><select value={type} onChange={(e)=>setType(e.target.value)} className="w-full rounded-xl border p-3"><option value="message">Direct Message</option><option value="report">Report</option>{isDirector&&<option value="assignment">Assignment</option>}{isDirector&&<option value="follow_up">Follow-up Instruction</option>}<option value="proposal">Proposal</option></select><input value={subject} onChange={(e)=>setSubject(e.target.value)} placeholder="Subject" className="w-full rounded-xl border p-3"/><textarea value={body} onChange={(e)=>setBody(e.target.value)} placeholder="Write your message, report, assignment, or instructions..." rows={8} className="w-full rounded-xl border p-3" required/>{type==="assignment"&&<label className="block text-sm font-bold">Assignment due date<input type="datetime-local" value={due} onChange={(e)=>setDue(e.target.value)} className="mt-1 w-full rounded-xl border p-3"/></label>}<label className="block text-sm font-bold">Follow-up date (optional)<input type="datetime-local" value={followUp} onChange={(e)=>setFollowUp(e.target.value)} className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-bold">Attachments and proposals<input type="file" multiple onChange={(e)=>setFiles(e.target.files)} className="mt-1 w-full rounded-xl border p-3"/></label><button disabled={busy} className="w-full rounded-xl bg-[#06245c] p-4 text-lg font-bold text-white disabled:opacity-60">{busy?"Sending...":"Send to Emanon Conversation"}</button></form></section></div>}</div></main>;
}
